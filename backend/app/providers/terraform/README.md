# Terraform Provider

Implementation lives in this package (`client.py`, `volume_provider.py`).

For the GitOps integration pattern, CI/CD requirements, and YAML conventions, see
[Terraform Integration (GitOps Pattern)](../../../../docs/TERRAFORM_GITOPS.md).
