{
  "key": "tag_change",
  "name": "tag_change",
  "complete_fact": "pr_merged",
  "stages": [
    {
      "kind": "step",
      "name": "open_pr",
      "tool": "open_tag_change_pr",
      "args": {
        "dataset_name": {"$var": {"path": "dataset_name", "default": ""}},
        "tags_sql": {"$var": {"path": "tags_sql", "default": ""}},
        "submitted_at": {"$var": {"path": "submitted_at", "default": null}},
        "requested_by": {"$var": {"path": "requested_by", "default": null}},
        "requested_by_email": {"$var": {"path": "requested_by_email", "default": null}},
        "changes": {"$var": {"path": "changes", "default": []}},
        "pr_title": {"$var": {"path": "pr_title", "default": null}}
      }
    },
    {
      "kind": "gate",
      "name": "pr_open",
      "type": "pr_merge",
      "waiting_status": "provisioning"
    }
  ]
}
