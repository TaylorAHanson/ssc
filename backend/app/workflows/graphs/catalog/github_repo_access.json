{
  "key": "github_repo_access",
  "name": "github_repo_access",
  "complete_fact": "access_request_issued",
  "stages": [
    {
      "kind": "step",
      "name": "issue_request_link",
      "tool": "request_github_access",
      "success_fact": "access_request_issued",
      "args": {
        "target_type": {"$var": {"path": "target_type", "default": "repo"}},
        "target": {"$coalesce": [{"$var": "target"}, {"$var": "repo_name"}, {"$var": "team"}]},
        "permission": {"$var": "permission"}
      }
    }
  ]
}
